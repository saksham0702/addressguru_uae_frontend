 <div className="overflow-x-auto">
        <table className="w-full mt-3 ">
          <thead className=" ">
            <tr className="">
              <th className="px-6 text-left text-xs font-semibold text-gray-500 capitalize tracking-wider">
                Listing Title
              </th>
              <th className="px-6 text-left text-xs font-semibold text-gray-500 capitalize tracking-wider">
                Status
              </th>
              <th className="px-6 text-left text-xs font-semibold whitespace-nowrap text-gray-500 capitalize tracking-wider">
                Created at
              </th>
              <th className="px-6 text-left text-xs font-semibold text-gray-500 capitalize tracking-wider">
                Packages
              </th>
              <th className="px-6 text-left text-xs font-semibold text-gray-500 capitalize tracking-wider">
                Upgrade
              </th>
              <th className="px-6 text-left text-xs font-semibold whitespace-nowrap text-gray-500 capitalize tracking-wider">
                Profile Strength
              </th>
              {/* <th className="px-6 text-left text-xs font-semibold text-gray-500 capitalize tracking-wider">
                Actions
              </th> */}
            </tr>
          </thead>
          <tbody className="bg-white divide-y divide-gray-100">
            {listings?.map((listing) => (
              <tr key={listing.id} className="hover:bg-gray-50">
                {/* Listing Title */}
                <td className="p-4 ">
                  <div className="flex items-center">
                    <div className="flex-shrink-0 h-10 w-10">
                      {listing.hasImage ? (
                        <img
                          className="h-10 w-10 rounded object-cover"
                          src={listing.image}
                          alt=""
                        />
                      ) : (
                        <div className="h-10 w-10 bg-blue-100 rounded flex items-center justify-center">
                          <span className="text-blue-600 font-medium text-xs">
                            DSOM
                          </span>
                        </div>
                      )}
                    </div>
                    <div className="ml-4">
                      <div className="text-sm font-semibold w-44 2xl:w-60">
                        {listing.title}
                      </div>
                    </div>
                  </div>
                </td>

                {/* Status */}
                <td className="px-4 py-4 whitespace-nowrap">
                  <span className="px-2 inline-flex text-xs leading-5 font-medium rounded-full text-[#1C1C1C]">
                    {listing.status}
                  </span>
                </td>

                {/* Created At */}
                <td className="px-6 py-4 2xl:whitespace-nowrap text-xs text-[#1C1C1C]">
                  {listing.createdAt}
                </td>

                {/* Packages */}
                <td className="px-6 py-4 whitespace-nowrap text-xs text-[#1C1C1C]">
                  {listing.packages}
                </td>

                {/* Upgrade */}
                <td className="px-6 py-4 whitespace-nowrap">
                  {listing.upgrade && (
                    <div className="flex items-center">
                      <div className="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
                      <span className="text-xs font-semibold">
                        {listing.upgrade}
                      </span>
                    </div>
                  )}
                </td>

                {/* Profile Strength */}
                <td className="px-6 py-4 whitespace-nowrap">
                  <CircularProgress percentage={listing.profileStrength} />
                </td>

                {/* Actions */}
                <td className="px-6 py-4 whitespace-nowrap text-right text-xs font-medium">
                  <div className="flex space-x-2">
                    <button className="inline-flex items-center px-2 py-1 border border-blue-300 rounded-md text-xs font-medium text-blue-700 bg-blue-50 hover:bg-blue-100 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                      <Edit className="w-3 h-3 mr-1" />
                      EDIT
                    </button>
                    <button className="inline-flex items-center px-2 py-1 border border-red-300 rounded-md text-xs font-medium text-red-700 bg-red-50 hover:bg-red-100 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                      <Trash2 className="w-3 h-3 mr-1" />
                      DELETE
                    </button>
                  </div>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>